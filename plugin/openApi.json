{
  "openapi": "3.0.3",
  "info": {
    "title": "AJRM Marine Tidal Database",
    "version": "0.7.0",
    "description": "Provider-neutral tidal predictions, provider-station mappings, secondary-port corrections, tidal-region relationships and persistent event caches. Spatial Locations remain owned by Location Editor."
  },
  "paths": {
    "/status": {
      "get": {
        "summary": "Inspect providers, cached stations and tidal ports",
        "responses": {
          "200": {
            "description": "Database status"
          }
        }
      }
    },
    "/definitions": {
      "get": {
        "summary": "Read tidal prediction, correction and area definitions",
        "responses": {
          "200": {
            "description": "Definition catalogue"
          }
        }
      }
    },
    "/definitions/ports/{locationId}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/LocationId"
        }
      ],
      "put": {
        "summary": "Create or replace a tidal definition for a spatial Location",
        "responses": {
          "200": {
            "description": "Definition saved"
          }
        }
      },
      "delete": {
        "summary": "Remove a tidal definition while retaining its spatial Location",
        "responses": {
          "200": {
            "description": "Definition removed"
          }
        }
      }
    },
    "/definitions/areas/{locationId}": {
      "parameters": [
        {
          "$ref": "#/components/parameters/LocationId"
        }
      ],
      "put": {
        "summary": "Create or replace the serving-port assignment for a tidal-region Location",
        "responses": {
          "200": {
            "description": "Area assignment saved"
          }
        }
      },
      "delete": {
        "summary": "Remove a tidal-region assignment while retaining its spatial Location",
        "responses": {
          "200": {
            "description": "Area assignment removed"
          }
        }
      }
    },
    "/stations/update": {
      "post": {
        "summary": "Update stations that are more than 24 hours old",
        "responses": {
          "200": {
            "description": "Updated database status"
          }
        }
      }
    },
    "/tides/status": {
      "get": {
        "summary": "Resolve a tide projection by position or stable port Location id",
        "responses": {
          "200": {
            "description": "Tide resolver projection"
          }
        }
      }
    },
    "/tides/pin": {
      "post": {
        "summary": "Set or clear the selected-port override",
        "responses": {
          "200": {
            "description": "Updated tide projection"
          }
        }
      }
    },
    "/tides/refresh": {
      "post": {
        "summary": "Resolve and refresh if the selected station is due",
        "responses": {
          "200": {
            "description": "Updated tide projection"
          }
        }
      }
    }
  },
  "components": {
    "parameters": {
      "LocationId": {
        "name": "locationId",
        "in": "path",
        "required": true,
        "schema": {
          "type": "string",
          "minLength": 1
        },
        "description": "Stable Location Editor id; display names are never identifiers."
      }
    },
    "schemas": {
      "Error": {
        "type": "object",
        "required": [
          "error"
        ],
        "properties": {
          "error": {
            "type": "string"
          }
        }
      }
    }
  }
}
