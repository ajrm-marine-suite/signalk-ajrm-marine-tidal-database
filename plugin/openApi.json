{
  "openapi": "3.0.3",
  "info": { "title": "AJRM Marine Tidal Database", "version": "0.1.0" },
  "paths": {
    "/status": { "get": { "summary": "Inspect providers, cached stations and tidal ports", "responses": { "200": { "description": "Database status" } } } },
    "/definitions": { "get": { "summary": "Read tidal prediction and correction definitions", "responses": { "200": { "description": "Definition catalogue" } } } },
    "/definitions/ports/{locationId}": {
      "put": { "summary": "Create or replace a tidal definition for a spatial Location", "responses": { "200": { "description": "Definition saved" } } },
      "delete": { "summary": "Remove a tidal definition while retaining its spatial Location", "responses": { "200": { "description": "Definition removed" } } }
    },
    "/stations/update": { "post": { "summary": "Update stations that are more than 24 hours old", "responses": { "200": { "description": "Updated database status" } } } },
    "/tides/status": { "get": { "summary": "Resolve a tide projection by position or port", "responses": { "200": { "description": "Tide resolver projection" } } } },
    "/tides/pin": { "post": { "summary": "Set or clear the selected-port override", "responses": { "200": { "description": "Updated tide projection" } } } },
    "/tides/refresh": { "post": { "summary": "Resolve and refresh if the selected station is due", "responses": { "200": { "description": "Updated tide projection" } } } }
  }
}
